<dialog
  id="modal"
  class="rounded-lg shadow-100vw bg-white p-5 w-1/2 [&[open]]:animate-fade-in [&[closing]&[open]]:animate-fade-out"
  x-data="modal"
  @modal-close="close"
  @htmx:after-swap="show"
>
</dialog>
