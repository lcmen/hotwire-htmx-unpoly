<!DOCTYPE html>
<html>
  <head>
    <title>Hotwire vs HTMX vs Unpoly</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  </head>

  <body>
    <main class="container mx-auto mt-28 px-5 flex">
      <div class="w-full">
        <div class="flex justify-center">
          <div class="w-1/2">
          <h1 class="text-3xl font-bold text-center mb-2 text-blue-500">Hotwire vs HTMX vs Unpoly</h1>
          <p class="text-center">This is a simple TODO application to compare Hotwire, HTMX and Unpoly.</p>
        </div>
      </div>
      <div>
        <p class="text-center mt-2">Click on the links below to see the selected technology in action.</p>
      </div>
      <div class="flex justify-center mt-8">
        <div class="w-1/2">
          <div class="flex justify-between space-x-4">
            <%= link_to "Hotwire", hotwire_path,
              class: "bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded w-1/3 text-center" %>
            <%= link_to "HTMX", htmx_path,
              class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded w-1/3 text-center" %>
            <%= link_to "Unpoly", unpoly_path,
              class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded w-1/3 text-center" %>
          </div>
          <div class="flex justify-center space-x-4 mt-8">
            <%= link_to "Inertia.js", inertia_path,
              class: "bg-purple-500 hover:bg-purple-700 text-white font-bold py-2 px-4 rounded w-1/3 text-center" %>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
