<% content_for :title, "Tasks" %>

<div class="w-full">
  <div class="mb-3 flex justify-between items-center">
    <h1 class="font-bold text-4xl">Tasks</h1>
    <%= link_to "New task", new_task_path,
      class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium",
      data: {
        hx_get: new_task_path,
        hx_push_url: true,
        hx_select: "#new-task",
        hx_swap: 'innerHTML',
        hx_target: '#modal',
      } %>
  </div>

  <table id="tasks" class="table-fixed min-w-full">
    <thead class="bg-gray-200">
      <tr>
        <th class="px-4 py-2 text-left text-gray-700">Status</th>
        <th class="w-2/5 px-4 py-2 text-left text-gray-700">Title</th>
        <th class="w-2/5 px-4 py-2 text-left text-gray-700">Description</th>
        <th class="w-1/5 min-w-60 px-4 py-2 text-left text-gray-700"></th>
      </tr>
    </thead>
    <tbody>
      <% @tasks.each do |task| %>
        <tr class="border-b">
          <td class="px-4 py-2 text-gray-600"><%= task.done %></td>
          <td class="px-4 py-2 text-gray-600"><%= task.title %></td>
          <td class="px-4 py-2 text-gray-600"><%= task.description %></td>
          <td class="px-4 py-2 text-gray-600 text-right">
            <%= link_to "Edit", edit_task_path(task, variant: :htmx),
              class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium",
              data: {
                hx_get: edit_task_path(task, variant: :htmx),
                hx_push_url: true,
                hx_select: "#edit-task",
                hx_swap: 'innerHTML',
                hx_target: '#modal',
              } %>
            <%= link_to "Destroy", task_path(task, variant: :htmx),
              class: "rounded-lg py-3 px-5 bg-red-600 text-white inline-block font-medium",
              data: { 
                hx_confirm: "Are you sure?",
                hx_delete: task_path(task, variant: :htmx),
                hx_select: '#tasks',
                hx_target: '#tasks',
              } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
